<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a2019.aoc.christina.christinaaoc2019">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ShopListViewActivity"></activity>
        <activity android:name=".InformationActivity" />
        <activity android:name=".CouponListViewActivity" />
        <activity android:name=".MainCounterActivity" />
        <activity android:name=".SelectCouponActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".AlertActivity" />
        <activity android:name=".SetGoalActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
    </application>
    <uses-feature
        android:name="android.hardware.sensor.stepdetector"
        android:required="true" />

    <uses-feature
        android:name="android.hardware.sensor.stepcounter"
        android:required="true" />

</manifest>